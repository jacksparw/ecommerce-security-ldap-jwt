spring.application.name=security-service

spring.cloud.config.uri=http://${CONFIG_SERVER_HOST:localhost}:${CONFIG_SERVER_PORT:5000}

spring.cloud.config.fail-fast=true
spring.cloud.config.retry.max-attempts=20